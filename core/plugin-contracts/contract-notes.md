# Contract Notes

The schema introduces a stable boundary between plugins and the agent framework.

Plugins declare capabilities, skills and evaluation hooks so routing and validation can remain independent.
