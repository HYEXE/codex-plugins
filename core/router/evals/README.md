# Routing Evaluation

Routing cases are stored as JSONL fixtures.

Each case defines an input request and expected plugin selection.

The dataset supports deterministic regression testing before introducing probabilistic routing.
