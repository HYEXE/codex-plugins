interface:
  display_name: "Prompt Compiler"
  short_description: "요청 점검·후속 변경분 반영·승인 경계·실행 검증"
  default_prompt: "Use $prompt-compiler to refine my request, preserve settled constraints, apply only the current delta, invalidate stale approvals or verification, execute, and verify."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
